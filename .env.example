# Myfxbook credentials for server-side sync via /api/myfxbook/sync.
# Configure these in Vercel project settings (Production + Preview).
# If absent, the app falls back to the last verified static values.

MYFXBOOK_EMAIL=
MYFXBOOK_PASSWORD=
